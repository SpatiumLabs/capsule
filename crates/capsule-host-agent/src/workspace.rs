//! Host-side sandbox workspace directories and path validation.
//!
//! The implementation lives in `capsule-core` so both host-agent and
//! sandboxd share one workspace management path.

pub use capsule_core::workspace::*;
